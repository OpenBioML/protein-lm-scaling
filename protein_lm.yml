name: protein_lm_env
channels:
  - pytorch
  - huggingface
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - numpy
  - pytorch
  - pydantic>=2.0
  - pip:
      - transformers
      - datasets
      - accelerate
